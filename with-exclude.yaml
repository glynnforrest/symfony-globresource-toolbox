services:
    _defaults:
        autowire: true
        autoconfigure: true
        public: false

    # glob loader searches src/NoLoad, even though it is excluded
    App\:
        resource: 'src/*'
        exclude: 'src/{NoLoad}'
